#1. security - api) social
#2. poenai api
#3. 포토폴리오에서 제일 필요로 하는 것들

웹풀스택 개발자 포트폴리오를 작성중인데 , 내가 맡은 파트 프로젝트,태스크파트야 실무진들이 봤을때 
아 이 개발자 진짜로 뽑고 싶다라는 생각이 들게 내가 맡은파트에서 ai활용또는 api들을 이용해서 
넣을 수 있는 아이디어 3가지 뽑아줘

1. LLIM 기반 "프로젝트 일정 리스크 예측 어시스턴트"
프로젝트 관리에서 가장 중요한 것은 단순한 진행률이 아니라 일정 지연 위험을 사전에 예측하는 것입니다
ai가 프로젝트 현황을 분석하여 관리자가 놓칠 수 있는 리스크를 미리 알려주는 기능입니다
구현아이디어
-프로젝트 진행률
-남은 기간
-완료된 태스크 비율
-담당자별 업무량
-지연된 업무 건수
예시:현재 프로젝트는 진행률 25%대비 남은 기간이 3일로 일정 지연 가능성이 높습니다. 담당자 A의 업무가 집중되어 있으므로 업무 재분배를 권장합니다.

2. Ai 기반 업무 난이도 및 예상 기간 추천 시스템
태스크 등록시 개발자가 직접 기간을 입력하는 대신, ai가 업무 내용을 분석하여 예상 소요 기간과 난이도를 추천해 주는 기능입니다.
구현 아이디어
사용자가 업무명을 입력합니다.
plain text
```
회원 관리 API 개발
JWT 로그인 기능 구현
프로젝트 검색 기능 추가
OPENAI API를 활용하여
예상 난이도, 권장 우선순위, 예상 개발 기간을 자동으로 추천

3.Slack,Discord 기반 실시간 업무 알림 시스템
구현아이디어
-태스크 등록
-담당자 배정
-마감일 임박
-업무 완료
-프로젝트 상태 변경

예시:
plain text
```
[sberp]
김다영님에게 새로운 업무가 배정되었습니다.
업무: 로그인 api 개발
마감일 : ~까지
```
slack webhook api또는 discord webhook api를 활용하여 실시간 알림 전송

---

오라클 테이블 구성

create table project(
    pro_id number not null,
    emp_id number not null,
    com_id number not null,
    pro_name varchar2(100) not null,
    pro_desc varchar2(1000),
    pro_status varchar2(20) not null,
    start_date date not null,
    end_date date,
    created_at date,
    updated_at date,

  CONSTRAINT pk_project primary key (pro_id),
  CONSTRAINT ck_project_status check(pro_status in ('todo','doing','done')),
  CONSTRAINT fk_project_employee1 FOREIGN KEY (emp_id)
  REFERENCES employee (emp_id),
  CONSTRAINT fk_project_company1 FOREIGN KEY (com_id)
  REFERENCES company(com_id)
);

-- 시퀀스
CREATE SEQUENCE seq_project START WITH 1 INCREMENT BY 1 NOCACHE NOCYCLE;

-- 트리거
CREATE OR REPLACE TRIGGER trg_project_bi
BEFORE INSERT ON project
FOR EACH ROW
BEGIN
    IF :NEW.pro_id IS NULL THEN
        SELECT seq_project.NEXTVAL
        INTO :NEW.pro_id
        FROM dual;
    END IF;
END;


create table project_member(
pm_id number not null,
project_pro_id number not null,
emp_id number not null,
role varchar2(50) not null,
joined_at date not null,
CONSTRAINT pk_project_member primary key (pm_id),
CONSTRAINT fk_project_member_project1 FOREIGN KEY(project_pro_id) REFERENCES project (pro_id),
CONSTRAINT fk_project_member_employee1 FOREIGN KEY (emp_id) REFERENCES employee (emp_id)
)

-- 시퀀스
CREATE SEQUENCE seq_project_member START WITH 1 INCREMENT BY 1 NOCACHE NOCYCLE;

-- 트리거
CREATE OR REPLACE TRIGGER trg_project_member_bi
BEFORE INSERT ON project_member
FOR EACH ROW
BEGIN
    IF :NEW.pm_id IS NULL THEN
        SELECT seq_project_member.NEXTVAL
        INTO :NEW.pm_id
        FROM dual;
    END IF;
END;


creatae table task(
task_id number not null,
pro_id number not null,
pm_id number not null,
com_id number not null,
task_name varchar2(100) ,
task_desc varchar2(1000) ,
task_status varchar2(20) not null,
pm_id_name varchar2(50) not null,
task_start_date date,
task_end_date date,
task_created_at date not null,
task_updated_at date not ull,
CONSTRAINT pk_task primary key (task_id),
CONSTRAINT fk_task_project1 FOREIGN KEY(pro_id) REFERENCES project(pro_id),
CONSTRAINT fk_task_company1 FOREIGN KEY(com_id) REFERENCES company(com_id),
CONSTRAINT fk_task_project_member FOREIGN KEY(pm_id) REFERENCES project_member(pm_id),
CONSTRAINT ck_task_status check(task_status in ('todo','doing','done'))
);

-- 시퀀스
CREATE SEQUENCE seq_task START WITH 1 INCREMENT BY 1 NOCACHE NOCYCLE;

--트리거
CREATE OR REPLACE TRIGGER trg_task_bi
BEFORE INSERT ON task
FOR EACH ROW
BEGIN
    IF :NEW.task_id IS NULL THEN
        SELECT seq_task.NEXTVAL
        INTO :NEW.task_id
        FROM dual;
    END IF;
END;

