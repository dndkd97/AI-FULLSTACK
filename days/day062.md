1. 테이블/시퀀스
```
create table appuser(
APP_USER_ID NUMBER(5) NOT NULL,
EMAIL VARCHAR2(100) NOT NULL,
PASSWORD VARCHAR2(100),
MBTI_TYPE_ID NUMBER(3),
CREATED_AT DATE,
UFILE VARCHAR2(255),
MOBILE VARCHAR2(50),
NICKNAME VARCHAR2(50),
PROVIDER VARCHAR2(50) NOT NULL,
PROVIDER_ID VARCHAR2(100)
);

create table authorities(
AUTH_ID NUMBER(5) NOT NULL,
EMAIL VARCHAR2(255),
AUTH VARCHAR2(255) NOT NULL,
APP_USER_ID NUMBER(5)
);

create SEQUENCE appuser_seq;
create SEQUENCE authorities_seq; // 시퀀스
```
2. sql  해당 sql
--1) 회원가입
insert into appuser(APP_USER_ID,EMAIL,PASSWORD,MBTI_TYPE_ID,CREATED_AT,UFILE,MOBILE,NICKNAME,PROVIDER,PROVIDER_ID) values
(appuser_seq.nextval,'frist@gamil.com','111',1,sysdate,'1.png','0101111','first','the703','t71');
--2) 로그인 
select u.email,u.password,a.auth
from appuser u left join authorities a on u.email = a.email
where u.email = 'first@gmail.com';
--- 이메일로 이메일, 비밀번호, 권한
--3) 이메일로 유저찾기
select * from appuser where email='frist@gamil.com';
--4) 이메일로 중복검사
select count(*) from appuser where email='frist@gamil.com';
--5) 회원수정
update appuser set 
password='2222',
MBTI_TYPE_ID=2,
ufile='2.png',
nickname='secound',
mobile='01022222',
provider='naver',
provider_id='n-1'
where app_user_id=1;
--6) 회원삭제
delete from appuser where app_user_id=1;
--7) 권한삽입
insert into authorities(AUTH_ID,EMAIL,AUTH)values(authorities_seq.nextval,'first@gmail.com','ROLE_MEMBER');
--8) 권한삭제
delete from authorities where email= 'first@gmail.com'; 