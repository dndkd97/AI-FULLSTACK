// 권한에 맞는 메뉴만 필터링
```
return allMenus.stream()
.filter(menu -> roles.contains(menu.role))
.map(menu -> menu.name)
.collect(Collectors.toList());
```
```
<div class="dropdown">
    <button>메뉴</button>
    <div class="dropdown-content" id="menuList"></div>
</div>

<script>
document.addEventListener("DOMContentLoaded", function () {
fetch("/api/menu")
.then(response => {
if (!response.ok) throw new Error("서버 응답 오류");
return response.json();
})
.then(data => {
const menuList = document.getElementById("menuList");
menuList.innerHTML = "";
data.forEach(item => {
const link = document.createElement("a");
link.href = "#";
link.textContent = item;
menuList.appendChild(link);
});
})
.catch(error => console.error("메뉴 불러오기 실패:", error));
});
</script>
```
```
<select name=”fruit” id=”fruit-select”>
  <option value=””>과일을 선택하세요</option>
  <option value=”apple”>사과</option>
  <option value=”banana” selected>바나나</option>
  <option value=”grape” disabled>포도(품절)</option>
</select>

selected = 기본 선택값
disabled = 선택 불가
value = 실제 전송 값
```

프로젝트
project(
projectId(PK)  
projectName    
description    
creator  
projectStatus 
participants  
startDate    
endDate    
createAt
)
테스크
task(
taskId(PK)
projectId(FK) 
taskName
taskStatus
)

taskId(PK) | projectId(FK) | taskName    | taskStatus
    1             1          '로그인구현'     'TODO'
    2             1         '회원가입구현'    'DOING'

---
projectId| projectName |  description  |creator|projectStatus|participants|  startDate  |   endDate   |   createAt   |
    1    |  'ERP개발'  |'ERP 시스템 구축' |'최다영'|   'TODO'   |      3      |'2026-06-13'| '2026-06-14' | '2026-06-12' |
    2    |'쇼핑몰 리뉴얼'| 'UI및 개능개선' |'김민지'|   'DOING'   |     4      |'2026-06-15'| '2026-06-16' | '2026-06-12' |


{projectId} projectName
{projectId} description
{projectId} creator
{projectId} projectStatus
{projectId} participants
{projectId} startDate
{projectId} endDate

1. 최다영

| ★6.  프로젝트 관리 | 사용자 | 관리자 |
| --- | --- | --- |
| CREATE | 프로젝트 등록  | 프로젝트 등록  |
| READ | 프로젝트 전체(조건별)조회, 상세 조회, 진행 상태(필터) 확인 | 프로젝트 전체(조건별)조회, 상세 조회, 진행 상태(필터) 확인 |
| UPDATE | 프로젝트 수정(자신회사) | 프로젝트 수정(모든회사) |
| DELETE | 프로젝트 삭제(자신회사) | 프로젝트 삭제(모든회사) |


| pro_id(pk) | com_id(fk) | pro_name | pro_desc | pro_creator(fk) | pro_status | start_date | end_date | created_at | updated_at |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 1 | ERP 전자결재 모듈 개발 | 전자결재 문서 및 결재선 기능 구현 | 1003 (김철수) | doing | 2026-06-10 | 2026-07-15 | 2026-06-10 | 2026-06-15 |
| 2 | 1 | 인사관리 시스템 개선 | 직급/재직 상태 공통코드 적용 | 1004 (이영희) | todo | 2026-07-01 | 2026-08-30 | 2026-06-16 | 2026-06-16 |
| 3 | 2 | 카카오 신규 서비스 기획 | 신규 서비스 기획 및 PoC 진행 | 2001 (박민수) | done | 2026-04-01 | 2026-06-01 | 2026-04-01 | 2026-06-16 |

create table project(
    pro_id INT AUTO_INCREMENT PRIMARY KEY,     -- 프로젝트 PK
    com_id INT NOT NULL,                       -- 회사 FK
    pro_name VARCHAR(100) NOT NULL,            -- 프로젝트 이름
    pro_desc TEXT,                             -- 프로젝트 설명
    emp_id INT NOT NULL,                       -- 생성자 FK → employee.emp_id
    pro_status VARCHAR(20) NOT NULL,           -- 상태: todo / doing / done
    start_date DATETIME NOT NULL,              -- 시작일
    end_date DATETIME,                         -- 종료일
    created_at DATETIME NOT NULL,              -- 생성일
    updated_at DATETIME NOT NULL,              -- 수정일
    FOREIGN KEY(com_id) REFERENCES company(com_id)
      ON UPDATE CASCADE ON DELETE CASCADE,
    FOREIGN KEY(emp_no) REFERENCES employee(emp_no)
      ON UPDATE CASCADE ON DELETE CASCADE
);

insert into project (com_id, pro_name, pro_desc, pro_creator, pro_status, start_date, end_date, created_at, updated_at)
values
(1, 'ERP 전자결재 모듈 개발', '전자결재 문서 및 결재선 기능 구현', 1003, 'doing', '2026-06-10', '2026-07-15', '2026-06-10', '2026-06-15'),
(1, '인사관리 시스템 개선', '사원 직급/재직 상태 공통코드 적용', 1004, 'todo', '2026-07-01', '2026-08-30', '2026-06-16', '2026-06-16'),
(2, '카카오 신규 서비스 기획', '신규 서비스 기획 및 초기 PoC 진행', 2001, 'done', '2026-04-01', '2026-06-01', '2026-04-01', '2026-06-16');


---
create table project_member(
    pm_id INT AUTO_INCREMENT PRIMARY KEY,   -- 프로젝트 멤버 PK
    pro_id INT NOT NULL,                    -- 프로젝트 FK
    emp_id INT NOT NULL,                    -- 사원 FK
    role VARCHAR(50),                       -- 역할 (예: PM, DEV, QA)
    joined_at DATETIME NOT NULL,            -- 참여일
    FOREIGN KEY(pro_id) REFERENCES project(pro_id)
      ON UPDATE CASCADE ON DELETE CASCADE,
    FOREIGN KEY(emp_id) REFERENCES employee(emp_id)
      ON UPDATE CASCADE ON DELETE CASCADE
);

insert into project_member (pro_id, emp_id, role, joined_at)
values
(1, 1003, 'PM', '2026-06-10'),   -- 김철수: 프로젝트 생성자, PM
(1, 1010, 'DEV', '2026-06-11'),  -- 최다영: 로그인 구현 담당
(1, 1011, 'DEV', '2026-06-11'),  -- 최아영: 회원가입 구현 담당
(2, 1004, 'PM', '2026-06-16'),   -- 이영희: 인사관리 개선 프로젝트 PM
(3, 2001, 'PM', '2026-04-01'),   -- 박민수: 카카오 신규 서비스 기획 PM
(3, 2002, 'ANALYST', '2026-04-02'); -- 박지훈: 기획안 작성 담당

| pm_id | pro_id | emp_id | role | joined_at |
| --- | --- | --- | --- | --- |
| 1 | 1 | 1003 | PM | 2026-06-10 |
| 2 | 1 | 1010 | DEV | 2026-06-11 |
| 3 | 1 | 1011 | DEV | 2026-06-11 |
| 4 | 2 | 1004 | PM | 2026-06-16 |
| 5 | 3 | 2001 | PM | 2026-04-01 |
| 6 | 3 | 2002 | ANALYST | 2026-04-02 |


---


| task_id | pro_id | com_id | task_name | task_desc | task_status | task_creator(pm_id) | task_start_date | task_end_date | task_created_at | task_updated_at |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 1 | 1 | 로그인 구현 | Spring Security 기반 로그인 기능 | todo | 2 | 2026-06-17 | 2026-06-22 | 2026-06-17 | 2026-06-17 |
| 2 | 1 | 1 | 회원가입 구현 | 회원정보 입력 및 검증 로직 개발 | doing | 3 | 2026-06-17 | 2026-06-22 | 2026-06-17 | 2026-06-18 |
| 3 | 1 | 1 | DB 스키마 설계 | 전자결재 모듈 DB 스키마 설계 | done | 1 | 2026-06-10 | 2026-06-15 | 2026-06-10 | 2026-06-15 |


create table task(
    task_id INT AUTO_INCREMENT PRIMARY KEY,   -- 태스크 PK
    pro_id INT NOT NULL,                      -- 프로젝트 FK
    com_id INT NOT NULL,                      -- 회사 FK
    task_name VARCHAR(100) NOT NULL,          -- 태스크 이름
    task_desc TEXT,                           -- 태스크 설명
    task_status VARCHAR(20) NOT NULL,         -- 상태: todo / doing / done
    pm_id INT NOT NULL,                       -- 작성자 FK → project_member.pm_id
    pm_id_name VARCHAR(50) NOT NULL,          -- 작성자 이름 (비정규화 필드)
    task_start_date DATETIME,                 -- 시작일
    task_end_date DATETIME,                   -- 종료일
    task_created_at DATETIME NOT NULL,        -- 등록일
    task_updated_at DATETIME NOT NULL,        -- 수정일
    FOREIGN KEY(pro_id) REFERENCES project(pro_id)
      ON UPDATE CASCADE ON DELETE CASCADE,
    FOREIGN KEY(com_id) REFERENCES company(com_id)
      ON UPDATE CASCADE ON DELETE CASCADE,
    FOREIGN KEY(pm_id) REFERENCES project_member(pm_id)
      ON UPDATE CASCADE ON DELETE CASCADE
);


insert into task (pro_id, com_id, task_name, task_desc, task_status, task_creator, task_creator_name, task_start_date, task_end_date, task_created_at, task_updated_at)
values
(1, 1, '로그인 구현', 'Spring Security 기반 로그인 기능 개발', 'todo', 2, '최다영', '2026-06-17', '2026-06-22', '2026-06-17', '2026-06-17'),
(1, 1, '회원가입 구현', '회원정보 입력 및 검증 로직 개발', 'doing', 3, '최아영', '2026-06-17', '2026-06-22', '2026-06-17', '2026-06-18'),
(1, 1, 'DB 스키마 설계', '전자결재 모듈 DB 스키마 설계', 'done', 1, '김철수', '2026-06-10', '2026-06-15', '2026-06-10', '2026-06-15');


>  트리거 코드 (employee 이름 변경 시 task 테이블 자동 갱신)
create trigger trg_update_task_creator_name
after update on employee
for each row
begin
    update task t
    join project_member pm on t.task_creator = pm.pm_id
    set t.task_creator_name = new.emp_name
    where pm.emp_no = new.emp_no;
end;


1. task 테이블에 작성자 이름을 직접 저장 → 조회 시 JOIN 필요 없음.
2. employee 테이블에서 이름 변경 발생 → 트리거 실행.
3. 해당 emp_id와 연결된 project_member → task 테이블의 task_creator_name 자동 갱신.

```

create table project(
    pro_id INT AUTO_INCREMENT PRIMARY KEY,     -- 프로젝트 PK
    com_id INT NOT NULL,                       -- 회사 FK
    pro_name VARCHAR(100) NOT NULL,            -- 프로젝트 이름
    pro_desc TEXT,                             -- 프로젝트 설명
    emp_id INT NOT NULL,                       -- 생성자 FK → employee.emp_id
    pro_status VARCHAR(20) NOT NULL,           -- 상태: todo / doing / done
    start_date DATETIME NOT NULL,              -- 시작일
    end_date DATETIME,                         -- 종료일
    created_at DATETIME NOT NULL,              -- 생성일
    updated_at DATETIME NOT NULL,              -- 수정일
    FOREIGN KEY(com_id) REFERENCES company(com_id)
      ON UPDATE CASCADE ON DELETE CASCADE,
    FOREIGN KEY(emp_id) REFERENCES employee(emp_id)
      ON UPDATE CASCADE ON DELETE CASCADE
)

create table project_member(
    pm_id INT AUTO_INCREMENT PRIMARY KEY,   -- 프로젝트 멤버 PK
    project_pro_id INT NOT NULL,                    -- 프로젝트 FK
    emp_id INT NOT NULL,                    -- 사원 FK
    role VARCHAR(50),                       -- 역할 (예: PM, DEV, QA)
    joined_at DATETIME NOT NULL,            -- 참여일
    FOREIGN KEY(pro_id) REFERENCES project(pro_id)
      ON UPDATE CASCADE ON DELETE CASCADE,
    FOREIGN KEY(emp_id) REFERENCES employee(emp_id)
      ON UPDATE CASCADE ON DELETE CASCADE
)

create table task(
    task_id INT AUTO_INCREMENT PRIMARY KEY,   -- 태스크 PK
    pro_id INT NOT NULL,                      -- 프로젝트 FK
    com_id INT NOT NULL,                      -- 회사 FK
    task_name VARCHAR(100) NOT NULL,          -- 태스크 이름
    task_desc TEXT,                           -- 태스크 설명
    task_status VARCHAR(20) NOT NULL,         -- 상태: todo / doing / done
    pm_id INT NOT NULL,                       -- 작성자 FK → project_member.pm_id
    pm_id_name VARCHAR(50) NOT NULL,          -- 작성자 이름 (비정규화 필드)
    task_start_date DATETIME,                 -- 시작일
    task_end_date DATETIME,                   -- 종료일
    task_created_at DATETIME NOT NULL,        -- 등록일
    task_updated_at DATETIME NOT NULL,        -- 수정일
    FOREIGN KEY(pro_id) REFERENCES project(pro_id)
      ON UPDATE CASCADE ON DELETE CASCADE,
    FOREIGN KEY(com_id) REFERENCES company(com_id)
      ON UPDATE CASCADE ON DELETE CASCADE,
    FOREIGN KEY(pm_id) REFERENCES project_member(pm_id)
      ON UPDATE CASCADE ON DELETE CASCADE
)

select p.* e.emp_name from project p join employee e
on p.emp_id = e.emp_id
order by p.pro_id desc
limit #{start},#{end}

상태조회
select p.* e.emp_name from project p join employee e
on p.emp_id = e.emp_id
where p.pro_status = #{pro_status}
order by p.pro_id desc
1) Dto

2) Mapper
1page)    select (페이징) / 검색
2page)    insert
3page)    select (상세) / select ( 테스크목록 )
4page)    select (상세)  / update
5page)    delete 
---
6page)    select (참여인원리스트) / insert( 참여인원 등록 )
---

7page)    delete 
8page)    select( 테스크 )  / 검색 
9,10page)    insert
11page)   select (상세)   / [수정] select (상세)  , update  / [삭제]

3) Service
4) Controller